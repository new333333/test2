<?xml version="1.0"?>

<project name="slide" basedir="." default="war">
    <import file="../deploy-support.xml" />
	<target name="init">
		<echo message="##### SLIDE #####" />
        <property environment="env" />
        <property name="project.dir" value=".." />
        <property file="${project.dir}/app.server.${user.name}.properties" />
        <property file="${project.dir}/app.server.properties" />
        <property file="${project.dir}/build.${user.name}.properties" />
        <property file="${project.dir}/build.properties" />
		<property name="input.war.file" value="slide.war" />
		<property name="output.war.file" value="slide.war" />
		<property name="temp.dir" value="temp" />
        <taskdef resource="net/sf/antcontrib/antcontrib.properties">
            <classpath>
                <pathelement location="${project.dir}/lib/ant-contrib.jar" />
            </classpath>
        </taskdef>
	</target>

	<target name="clean" depends="init">
		<delete>
			<fileset dir="." includes="${output.war.file}" />
		</delete>
		<delete includeEmptyDirs="true" failonerror="false">
			<fileset dir="${temp.dir}" />
		</delete>
	</target>
		
	<target name="unwar-original" depends="init">
		<unwar src="original-kit/${input.war.file}" dest="${temp.dir}"/>
	</target>
	
	<target name="apply-changes" depends="unwar-original">
		<copy todir="${temp.dir}">
			<fileset dir="changes" includes="**/*" />
		</copy>
	</target>
	
	<target name="war" depends="apply-changes">		
		<war
			basedir="${temp.dir}"
			destfile="${output.war.file}"
			webxml="${temp.dir}/WEB-INF/web.xml"
			update="false"
		/>
	</target>
	
    <target name="deploy" depends="war">
        <antcall target="deploy-war">
        	<param name="deploy-support.war.file" value="${output.war.file}" />
        </antcall>
    </target>
	
</project>